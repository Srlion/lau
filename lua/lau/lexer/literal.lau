let enum = lau.include_file("lau/enum.lau");
let LITERAL, Literal = enum(
	s => "'" .. s.value .. "'",
	s => "Literal(" .. s.key:lower() .. "): " .. s
);

Literal("Bool");
Literal("Number");
Literal("String");
Literal("Nil",
	() => "nil",
	() => "Literal(nil)"
);

return LITERAL;